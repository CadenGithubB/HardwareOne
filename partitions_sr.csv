# Partition table for builds WITH ESP-SR (speech recognition)
# Factory: 4948K  (binary footprint ~4831K + ~117K headroom)
# Model:   3008K  Sized for WN9_HIESP (~292K) + MN7_EN_QUANT (~2.6M) +
#          fst (~12K). Packed srmodels.bin is ~2980K — 2944K (the prior
#          size) overflowed by 36K and silent-truncated the WN entry into
#          the next partition, so esp_srmodel_filter(WN_PREFIX) returned
#          NULL and srstart failed with "No wake word model found!". Bumped
#          to 3008K to give ~28K headroom. Chinese MultiNet (~3.5M) is
#          disabled in sdkconfig — a smaller model bundle is what makes
#          everything fit on the 8M chip.
# LittleFS: 172K  (was 236K — donated another 64K to the model partition
#          so srmodels.bin fits without truncation. Suits logs/cache;
#          bulk content should live on the SD card.)
# Name,   Type, SubType, Offset,   Size,    Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x4D5000,
model,    data, spiffs,  0x4E5000, 0x2F0000,
littlefs, data, littlefs, 0x7D5000, 0x2B000,
