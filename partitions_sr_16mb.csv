# Partition table for 16MB-flash boards WITH ESP-SR (speech recognition)
# Used by 16MB-flash boards (e.g. FeatherS3[D]) selected via HW_BOARD.
# Factory:  4992K (0x4E0000)  — same as 8MB SR layout for binary compatibility.
# Model:    3008K (0x2F0000)  — same as 8MB SR layout (WN9_HIESP + MN7_EN_QUANT + fst).
# LittleFS: 8320K (0x820000)  — ~8.1 MB, +8.2 MB vs the 8MB SR layout's 128K.
# Total:    ~16384K
# Flash-encryption prep (2026-07-30): nvs moved to 0xA000 and shrunk 24K->16K
# to make room for BOTH a 4K nvs_key partition (XTS keys for NVS encryption,
# mandatory in any CONFIG_SECURE_FLASH_ENC_ENABLED build) AND a partition
# table at 0x9000 - the FE-enabled bootloader is ~33K, over the 32K ceiling a
# 0x8000 table allows, so boards/feathers3_fe.defaults sets
# CONFIG_PARTITION_TABLE_OFFSET=0x9000. Plain boards keep the table at 0x8000
# and simply leave 0x8000-0xA000 slack; the same CSV serves both offsets.
# littlefs (and model, where present) carry the `encrypted` flag - a
# documented no-op without flash encryption. factory/littlefs offsets are
# UNCHANGED; the nvs move reformats NVS once (saved WiFi creds re-enter).
# Name,   Type, SubType, Offset,   Size,    Flags
nvs,      data, nvs,     0xA000,   0x4000,
nvs_key,  data, nvs_keys, 0xE000,  0x1000,  encrypted
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x4E0000,
model,    data, spiffs,  0x4F0000, 0x2F0000, encrypted
littlefs, data, littlefs, 0x7E0000, 0x820000, encrypted
