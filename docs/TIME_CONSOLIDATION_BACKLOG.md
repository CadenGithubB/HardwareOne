# Time-system consolidation — sweep results & backlog (2026-07-30)

Source: 4-agent read-only sweep (dup-formatters / dead-code / anchor-map / consumer-sweep).
Items marked DONE were implemented the same day (see git diff); everything else is a ranked backlog.
The anchor-map dimension was consumed during implementation and is omitted here except for its
unique findings.

## [DONE] [duplicate] ESP-NOW boot notification is built and sent TWICE on an autostart boot
- Occurrences: components/hardwareone/System_ESPNow.cpp:9997-10004 (inside initEspNow, ends :10009); components/hardwareone/HardwareOne.cpp:2150-2162
- Evidence: Both blocks are near-verbatim: `time_t now = time(nullptr); uint32_t timestamp = (now > 1609459200) ? now : 0; String bootMsg = buildBootNotification(generateMessageId(), gEspNow->deviceName.c_str(), gBootCounter, timestamp); meshSendBootToPeers(bootMsg);`. Call chain verified: HardwareOne.cpp:2140 `cmd_espnow_init("")` -> System_ESPNow.cpp:10058 `initEspNow()` -> sends at :10002; then HardwareOne.cpp:2151 `if (gEspNow && gEspNow->initialized)` sends again with a fresh msgId (so mesh dedup does not collapse them). Peers receive two boot notifications per autostart boot.
- Recommendation: Delete the HardwareOne.cpp:2150-2162 block entirely. initEspNow() is the right single home — its own comment at HardwareOne.cpp:2145 already documents the pattern that init-side placement covers every path (boot + manual `openespnow`). Fold the timestamp gate into buildBootNotification (or use Clock::isValidEpoch there) so the literal exists once (see the epoch-literal item).
- Risk: Behavioral change on the wire (peers stop seeing the duplicate); harmless but include in HW test. Wrapped in the same #if as ESP-NOW; no board gating differences between the two sites.

## [BACKLOG] [dead] All three Clock:: formatters (formatISO8601Local, formatFilenameLocal, formatHHMMLocal) have ZERO callers
- Occurrences: components/hardwareone/System_Clock.h:104, :109, :113 (decls); System_Clock.cpp:96, :108, :118 (impls). Repo-wide grep (all .cpp/.h/.c outside docs/) finds no call site.
- Evidence: grep -rn for the three names hits only System_Clock.h/.cpp. Meanwhile the firmware's de-facto filename stamp is "%Y-%m-%dT%H-%M-%S" (5 sites, next item) — NOT the "YYYYMMDD_HHMMSS" formatFilenameLocal emits — so the one helper built for this job produces a format nothing wants, and the header's claim of being 'the single source of truth' (System_Clock.h:22) is false for formatting.
- Recommendation: Do not delete — adopt. (a) Change formatFilenameLocal (or add formatCaptureStampLocal) to emit the de-facto "%Y-%m-%dT%H-%M-%S" and migrate the 5 capture-stamp sites; System_ImageManager.cpp:140 (`img_%04d%02d%02d_%02d%02d%02d.jpg`) is an exact-format adopter for the CURRENT YYYYMMDD_HHMMSS body, so decide the canonical shape first. (b) formatISO8601Local's exact pattern lives at System_MQTT.cpp:1214-style sites and cmd_time (plan-owned). (c) formatHHMMLocal has no natural adopter (G2_Health's HH:MM is deliberately UTC) — if nothing adopts it after the sweep, delete it rather than keep dead API.
- Risk: System_TimeAnchors.cpp:266 renames boot files to `base-STAMP` expecting the same shape System_SensorLogging writes — the capture-stamp family must migrate together or retro-dated filenames change shape mid-fleet. Coordinate with the parallel plan session (it edits System_Clock.*).

## [BACKLOG] [duplicate] WebPage_Sensors RTC handler re-implements rtcLocalTime() + day-of-week inline (3rd copy of UTC->local calendar math)
- Occurrences: components/hardwareone/WebPage_Sensors.cpp:469-536 (offset math :472-518, leap/daysInMonth inline twice :493-497 and :503-507, Zeller :520-533, day table :535); canonical: i2csensor_ds3231.cpp:303-308 rtcDaysInMonth, :310-314 rtcDayOfWeek (Sakamoto), :316-356 rtcLocalTime
- Evidence: WebPage_Sensors: `int totalMinutes = dt.hour * 60 + dt.minute + offsetMinutes; ... int daysInMonth[] = {31, 28, ...}; ... // Calculate day of week for local date using Zeller's congruence`. rtcLocalTime does the identical rollover with rtcDaysInMonth and Sakamoto DOW. Two different DOW algorithms (Zeller vs Sakamoto) for the same question. The web comment 'Manual calculation to avoid unsafe setenv/tzset' (:470) is stale — rtcLocalTime is pure arithmetic, no setenv.
- Recommendation: Replace WebPage_Sensors.cpp:469-518+520-536 with `RTCDateTime local = rtcLocalTime(&dt);` (declared in i2csensor_ds3231.h:87; the handler is already inside #if ENABLE_RTC_SENSOR). NOTE: the plan already schedules this exact deletion (docs/CLOCK_PLUMBING_AND_DST_PLAN.md line 217) but parks it in Stage 3 (DST), which is DEFERRED — calling rtcLocalTime now is behavior-identical, removes ~65 lines, and doesn't preempt the days_from_civil endgame.
- Risk: Board-gated: #if ENABLE_RTC_SENSOR — compile with CUSTOM_ENABLE_RTC 1 to prove it builds (board-gated code hides compile breaks). rtcLocalTime returns dayOfWeek 1=Sun; the web JSON wants a "Sun"-style name — index the shared day table with dow-1.

## [BACKLOG] [inconsistency] G2 lens shows RTC time in raw UTC while OLED/CLI/web show local — violates the ds3231 header's display contract
- Occurrences: components/hardwareone/G2_Page_Sensors.cpp:188-204 (rtcG2FormatValue, HH:MM from raw cache), :955-970 (g2BuildRtcReadout, full date/time from raw cache); contract: i2csensor_ds3231.h:84-86; compliant surfaces: i2csensor_ds3231_oled.h:37, i2csensor_ds3231.cpp:635 (cmd_rtc), WebPage_Sensors.cpp:469+ (via its own duplicate). Related raw-UTC emitters: System_MQTT.cpp:1213-1216, i2csensor_ds3231.cpp rtcBuildDataJSON (feeds `rtc json`, sensors json, ESP-NOW streaming)
- Evidence: i2csensor_ds3231.h:84-86: 'Returns a copy of utc with gSettings.tzOffsetMinutes applied... Use this before any human-facing display of RTC time.' rtcG2FormatValue: `snprintf(out, cap, "%02u:%02u", (unsigned)gRtcCache.dateTime.hour, ...)` — no rtcLocalTime call anywhere in G2_Page_Sensors.cpp. With tzOffsetMinutes != 0 the glasses' RTC tile and readout are wrong by the offset while every other surface is right.
- Recommendation: In both G2 builders, convert once: `RTCDateTime local = rtcLocalTime(&gRtcCache.dateTime);` (copy the cache fields under the guard, convert after release — rtcLocalTime is pure math). MQTT/JSON raw-UTC is machine-facing and defensible, but is unlabeled; if kept UTC, add a "utc":true or rename the key in a schema pass rather than converting (remote ESP-NOW viewers currently display it as-is).
- Risk: G2_Page_Sensors RTC code is #if ENABLE_RTC_SENSOR; test on a glasses-connected build. Not covered by the parallel plan (its tz census lists ds3231/WebPage/WiFi label but not G2_Page_Sensors).

## [DONE] [inconsistency] Five different magic epoch-validity thresholds still hand-rolled where Clock::isValidEpoch exists
- Occurrences: components/hardwareone/System_Debug.cpp:1693 (1000000000 = 2001); System_CrashRecord.cpp:418 (1600000000 = Sep 2020); System_ESPNow.cpp:3672 (1577836800) and :10000 (1609459200 = 2021); System_User.cpp:3097 (1577836800); System_Maps.cpp:2946 (1609459200); HardwareOne.cpp:2157 (1609459200); System_ImageManager.cpp:138 (1704067200 = 2024); System_WiFi.cpp:1211-1212 (tm_year+1900 >= 2020 form)
- Evidence: Same question — 'is the wall clock real?' — answered with 2001, Sep-2020, 2020, 2021, and 2024 depending on file. e.g. System_Debug.cpp:1693 `if (now > 0 && now > 1000000000) { // Valid epoch time (after year 2001)` vs System_ImageManager.cpp:138 `if (now > 1704067200) { // After 2024-01-01 (valid time)`. A clock stuck in 2015 makes the system log filename dated but the image filename counter-based.
- Recommendation: Replace all nine with Clock::isValidEpoch(now) (System_Clock.h:45; threshold 2020). Excluded as justified: System_G2_Protocol.cpp:1767 TS_MIN/TS_MAX (named constants, wire-frame range validation with an upper bound — keep, optionally define TS_MIN from a shared Clock constant); System_Utils.cpp's 1704067200×3 (plan Stage 1 replaces them); HardwareOne.cpp:1782 tm_year>=120 (documented deliberate re-use of the just-populated tm to avoid a re-read race).
- Risk: System_CrashRecord.cpp:418 runs at boot from normal task context (verified — it is the next-boot writer, not a panic handler), so calling isValidEpoch (gmtime_r) is safe. Threshold unification slightly changes acceptance at the margins (e.g. Debug now rejects 2001-2019 clocks) — all are wanted tightenings. System_ESPNow.cpp:3672's gate merges into the shared UTC formatter below.

## [BACKLOG] [duplicate] Capture-filename timestamp "%Y-%m-%dT%H-%M-%S" hand-rolled at 5 sites (+2 divergent variants)
- Occurrences: components/hardwareone/System_Debug.cpp:1697; System_SensorLogging.cpp:337 and :1907; System_Maps.cpp:2948; System_TimeAnchors.cpp:266; variants: WebServer_Server.cpp:4959 ("%Y-%m-%d_%H-%M-%S", automations backup download) and System_ImageManager.cpp:140 (img_YYYYMMDD_HHMMSS.jpg). Day-folder sibling "%Y-%m-%d" at System_SensorLogging.cpp:929, :1898 and System_TimeAnchors.cpp:264
- Evidence: Identical strftime pattern in five files, each with its own localtime plumbing, e.g. System_Maps.cpp:2946-2951 `if (now > 1609459200) { struct tm* ti = localtime(&now); strftime(timestamp, ..., "%Y-%m-%dT%H-%M-%S", ti); } else { snprintf(timestamp, ..., "%lu", millis()); }` — and each with a DIFFERENT dark-clock fallback (uptime-N / boot-counter / raw millis).
- Recommendation: One Clock:: helper emitting the canonical capture stamp (see the dead-formatters item) adopted at the 5 identical sites; the validity gate and localtime call move inside it. Leave the dark-clock fallbacks at call sites — they are genuinely policy-different (sensorlog's boot-counter scheme is load-bearing for the anchor sweep). WebServer's underscore variant only names a browser download — harmonize opportunistically. ImageManager should call the helper too (its 2024 literal and non-reentrant localtime go away with it).
- Risk: System_TimeAnchors renames boot-named files into this shape — SensorLogging and TimeAnchors MUST move in the same commit. Maps/ImageManager are board/feature-gated (GPS, camera): compile-check those configs.

## [BACKLOG] [duplicate] UTC ISO-8601 "Z" stamp built independently 4 times (two static helpers + two inline)
- Occurrences: components/hardwareone/System_User.cpp:2910-2927 (static formatEpochAsISO8601); System_User.cpp:1103-1106 (inline, updateUserLastSeen — does not use the helper 1800 lines away in its own file); System_R1_Protocol.cpp:422-431 (static formatEpoch); System_ESPNow.cpp:3669-3679 (inline, ESP-NOW user-sync createdAt)
- Evidence: All four: gmtime_r + `strftime(..., "%Y-%m-%dT%H:%M:%SZ", ...)`. Validity/sentinel handling differs per copy: formatEpochAsISO8601 writes "null" via Clock::isValidEpoch; R1's formatEpoch writes "n/a" on epoch==0 with NO validity check; the ESPNow inline uses the raw 1577836800 literal; updateUserLastSeen gates with Clock::isValidEpoch before formatting.
- Recommendation: Add Clock::formatISO8601UTC(time_t, char*, size_t) mirroring formatISO8601Local's contract (returns 0 + "" on invalid epoch). Keep the per-site sentinels ("null"/"n/a") at call sites keyed off the 0 return. This gives System_Clock its missing UTC formatter and deletes two static helpers and one magic literal.
- Risk: users.json createdAt/lastSeen strings are parsed elsewhere — format is unchanged, only the builder moves. R1's formatEpoch currently formats ANY nonzero epoch (e.g. a 1970-era ring stamp renders as 1970-01-01) — routing through the validating helper changes that output to "n/a"-style; that is more honest but verify the R1 debug consumers.

## [DONE] [dead] String formatDateTime(time_t) is dead code (and uses non-reentrant localtime + String return)
- Occurrences: components/hardwareone/System_Utils.cpp:673-678 (impl); System_Utils.h:335 (decl). Zero callers repo-wide.
- Evidence: `String formatDateTime(time_t timestamp) { struct tm* timeinfo = localtime(&timestamp); char buffer[32]; strftime(buffer, sizeof(buffer), "%Y-%m-%d %H:%M:%S", timeinfo); return String(buffer); }` — grep finds only the definition and declaration.
- Recommendation: Delete both lines' worth. If a String-returning local-time formatter is ever wanted, it should wrap Clock::formatISO8601Local, not exist as a fourth pattern.
- Risk: None — link-verified unused. Per the pre-1.0 audit rule, re-verified today by grep before recommending deletion.

## [BACKLOG] [duplicate] freshestVitalAge() duplicated VERBATIM in OLED and G2 health surfaces; fmtAge/fmtAgeShort near-duplicated with a real divergence
- Occurrences: components/hardwareone/OLED_Mode_R1_Health.cpp:53-65 and G2_Health.cpp:906-918 (identical bodies incl. the comment '// Freshest among valid vitals (min age). -1 if none.'); OLED_Mode_R1_Health.cpp:42-50 (fmtAgeShort) vs G2_Health.cpp:887-903 (fmtAge); web JS: WebPage_R1_Health.cpp:68-74 (age())
- Evidence: freshestVitalAge: byte-identical lambda-consider walk over hr/hrv/spo2/temp/battery in both files. fmtAgeShort ends `else snprintf(out, cap, "%ldh", (long)(ageSec / 3600));` — no day bucket, so a 3-day-old vital reads "72h" on OLED but "3d" on the glasses (G2_Health.cpp:898-901). Both take the same G2RingTelemetry.
- Recommendation: UI-layer, rank below the system items. Move freshestVitalAge into G2_Ring.h (it is a pure function of G2RingTelemetry — that header is the natural home) or G2_Health.h, and share ONE fmtAge (keeping the day bucket and the 5s anti-flicker bucketing) from the same place; OLED includes it. The web JS copy is justified (JS cannot call C) — leave it, though its format ('12s ago', no 5s bucket) could mirror the C string for cross-surface consistency.
- Risk: OLED and G2 modules are both always-compiled on feathers3; trivial. G2_Health.cpp carries live uncommitted health work — coordinate.

## [BACKLOG] [duplicate] Two more compact-age formatters (sys-events lens, OLED notifications) — and the OLED one silently shows NO age past 1 hour
- Occurrences: components/hardwareone/G2_Glasses.cpp:5046-5050 (buildSysEventsReadoutText: s/m/h); OLED_Utils.cpp:699-708 (notification age: '<60s -> "Ns ago", <3600 -> "Nm ago", >=3600 -> nothing')
- Evidence: OLED_Utils.cpp:700-708: `if (elapsed < 60) {..."s ago"...} else if (elapsed < 3600) {..."m ago"...}` — no else branch, so a notification older than an hour renders with no age at all. G2_Glasses:5048-5050 is a third s/m/h ladder distinct from fmtAge/fmtAgeShort.
- Recommendation: Fold into the same shared fmtAge from the previous item (suffix-less variant param or a second tiny fn). Fixing the OLED >=1h dropout falls out for free.
- Risk: Line-length budgets differ (G2 lens pads to %-3s); keep width concerns at call sites. Low risk, pure display.

## [BACKLOG] [duplicate] Clock-drift projection idiom duplicated between ring corrective push and glasses time-sync tick (both live uncommitted)
- Occurrences: components/hardwareone/G2_Ring.cpp:485-489; G2_Glasses.cpp:8463-8469
- Evidence: Both: `const int64_t expected = (int64_t)pushedEpoch + (int64_t)(((uint32_t)millis() - pushedAtMs) / 1000u); int64_t drift = (int64_t)now - expected; if (drift < 0) drift = -drift;` with the same 120s threshold and the same 'never pushed' epoch==0 special case.
- Recommendation: One small helper, e.g. `Clock::driftFromProjection(uint32_t pushedEpoch, uint32_t pushedAtMs)` returning |now - projected| seconds (callers keep their own 120s/never-pushed policy, which the plan marks deliberate). System_Clock.h is the right home — it already owns ageSeconds and the projection concept (P1.1 adds projectedEpochUs).
- Risk: HIGH coordination risk: both files carry live uncommitted custody work AND the parallel plan session edits System_Clock.*. Do this as part of that merge, not independently. Behavior must stay bit-identical (drift rules are HW-validated).

## [BACKLOG] [duplicate] Month-abbreviation table x2 and Sun..Sat day-name table x4 in C++
- Occurrences: Months: components/hardwareone/G2_Health.cpp:1148-1150 (kMon, 0-based); OLED_Mode_Automations.cpp:529 (monthAbbr[13], 1-based). Days: G2_Page_Sensors.cpp:954 (kRtcDow[8], 1-based); i2csensor_ds3231_oled.h:60 (days[], 1-based); WebPage_Sensors.cpp:535 (days[], 0-based); System_Automation.cpp:3023 (kDays uppercase, 0-based)
- Evidence: Four independent {"Sun","Mon",...} spellings with three different indexing conventions (0-based tm_wday, 1-based RTC dayOfWeek, uppercase) and two {"Jan",...} tables with different base indices.
- Recommendation: Shared `kMonthAbbrev[12]` and `kDayAbbrev[7]` (0-based, tm convention) in System_Clock.h; 1-based callers index [dow-1]. Keep as-is (justified): System_Automation.cpp:2197 lowercase names (parse table, not display), OLED_Mode_Automations.cpp:542-545 two-char Mo/Tu map (16-byte column budget), System_Automation kDays uppercase if the condition-variable string contract requires caps (it feeds automation string comparisons — check consumers before unifying), and the JS `days[]` in i2csensor_ds3231_web.h (client-side).
- Risk: Indexing-convention bugs are the whole risk — the 1-based RTC tables deliberately carry a "?" slot 0. Small win; do it in the same pass as the day-of-week dedup so conventions are decided once.

## [BACKLOG] [duplicate] Timezone-offset label ("UTC+H:MM") built twice; cmd_ntpstatus also hand-rolls the isSynced check
- Occurrences: components/hardwareone/System_WiFi.cpp:1205 (simple form) and :1208-1215 (getLocalTime + tm_year+1900>=2020 + strftime); i2csensor_ds3231.cpp:637-648 (richer form: UTC / UTC+H / UTC+H:MM)
- Evidence: WiFi: `snprintf(tzStr, sizeof(tzStr), "UTC%+d:%02d", tzMin / 60, abs(tzMin % 60));` — always prints minutes ("UTC+-5:00"); ds3231: three-branch version that drops ":00". Two label formats for the same setting on two CLI outputs. :1211-1212 re-derives Clock::isSynced by hand.
- Recommendation: Clock::formatTzOffsetLabel(char*, size_t) with the ds3231 three-branch behavior; both call it. Replace the :1211-1214 block with `if (Clock::isSynced())` + one strftime. NOTE: the plan already edits cmd_ntpstatus (System_WiFi.cpp:1217-1235, 'Time source' row + backup-servers fix) and flags the :1203 label as a DST-staleness site — land this with that edit, not separately.
- Risk: Pure display; the plan session owns adjacent lines — merge conflict risk is the main cost.

## [BACKLOG] [inconsistency] Non-reentrant localtime() at 8 sites in a firmware that otherwise standardized on localtime_r
- Occurrences: components/hardwareone/System_Automation.cpp:2860, :2935, :3022; System_Debug.cpp:1694; System_Maps.cpp:2947; System_ImageManager.cpp:139; System_Utils.cpp:674 (dead fn); WebServer_Server.cpp:4957
- Evidence: `struct tm* timeinfo = localtime(&now);` — every other time consumer in the tree uses localtime_r (40+ sites).
- Recommendation: Mechanical switch to localtime_r with a stack tm, mostly falling out of the other items (Debug/Maps/ImageManager via the shared capture-stamp helper; Utils:674 via deletion). System_Automation's three are in the condition-variable/scheduler path — fix during the current automation work.
- Risk: Honest severity note: ESP-IDF newlib's localtime writes into the per-task _reent tm, so this is NOT a cross-task race in practice — it is an inconsistency plus a small per-task first-use heap alloc, not an active bug. Rank low; don't sell it as a crash fix.

## [BACKLOG] [duplicate] Low-value long-tail: uptime h/m breakdown x7 and raw (millis()-x)/1000 x~25 despite Clock::ageSeconds
- Occurrences: Uptime: components/hardwareone/OLED_Mode_System.cpp:64-71, :152-159, :698-699; OLED_Mode_Network.cpp:800-801; OLED_Mode_Remote.cpp:142; G2_Glasses.cpp:3648-3663; System_Utils.cpp:1613-1617 (cmd_status uptime_hms) + :1856-1864 (cmd_uptime). Age math (representative, verified list): OLED_Mode_Remote.cpp:139; System_MQTT.cpp:864, :927, :931; OLED_Mode_System.cpp:152; System_ESPNow.cpp:10266, :10304, :11628, :11684, :12295, :15199; System_Debug.cpp:1789; System_SensorLogging.cpp:1136; System_Microphone.cpp:896; OLED_Utils.cpp:699; WebServer_Server.cpp:3139; System_WiFi.cpp:1674; System_I2C.cpp:1488; Bluetooth.cpp:1933; G2_Glasses.cpp:8464, :21313; G2_Ring.cpp:345, :486, :558
- Evidence: System_Clock.h:19 itself documents '"(millis() - timestamp) / 1000" repeated ~10 places' — the helper exists (ageSeconds, :86-88) but adoption stopped at G2_Ring/G2_Health/SensorLogging; the count is now ~25. Uptime: `hours = uptimeSec / 3600; minutes = (uptimeSec % 3600) / 60;` in six display files plus two shapes of "%luh %lum %lus" in System_Utils.
- Recommendation: Honest assessment: mass-converting every (millis()-x)/1000 to Clock::ageSeconds is churn with near-zero behavior delta (all copies already use unsigned math, so rollover behavior is identical) — adopt opportunistically when a file is already being touched, and use it in ALL NEW code. The uptime breakdown is worth one tiny helper (formatUptimeHM(char*,size_t,uint32_t) + an HMS variant) only because the surfaces are supposed to agree; the per-surface width differences ("Up: XhYm" vs "XhYm - NC") stay at call sites.
- Risk: Lowest rank. Do not let this family bloat the consolidation commit; the first 14 items are the value.

## [DONE] [dead] formatDateTime(): zero callers repo-wide, unguarded, non-reentrant — delete, don't guard
- Occurrences: components/hardwareone/System_Utils.cpp:675 (definition), components/hardwareone/System_Utils.h:356 (declaration). Zero call sites in components/, main/, tools/ (grep-verified twice, hours apart, across the live-edited tree).
- Evidence: String formatDateTime(time_t timestamp) {
  struct tm* timeinfo = localtime(&timestamp);
  char buffer[32];
  strftime(buffer, sizeof(buffer), "%Y-%m-%d %H:%M:%S", timeinfo);
  return String(buffer);
}
- Recommendation: DELETE both lines (guard-vs-delete question is moot: the caller enumeration came back empty, so there is no pre-sync-epoch exposure to guard). It is doubly obsolete: Clock::formatISO8601Local (System_Clock.h:104) is the guarded, reentrant (localtime_r) replacement for any future need, and this one uses non-reentrant localtime() plus a heap String return. Per the pre-1.0 audit rule this was re-verified immediately before reporting.
- Risk: None — no callers exist on any board config. If the parallel session's plan intended to migrate callers TO a shared formatter, the target is Clock::formatISO8601Local, not this.

## [DONE] [dead] nowEpoch(): zero callers, duplicates Clock::epochSeconds()
- Occurrences: components/hardwareone/System_Utils.cpp:2515 (definition, sits right after the #endif // ENABLE_WIFI), components/hardwareone/System_Utils.h:201 (declaration). Zero call sites repo-wide.
- Evidence: time_t nowEpoch() {
  return time(nullptr);
}  // System_Utils.cpp
// vs System_Clock.h:34: inline time_t epochSeconds() { return time(nullptr); }
- Recommendation: DELETE both lines. Clock::epochSeconds() is the identical, documented single copy (System_Clock.h is the declared single source of truth for time accessors).
- Risk: None — unconditionally compiled, so removal is safe on every board.

## [DONE] [dead] Five never-referenced local extern declarations of time/logging helpers
- Occurrences: components/hardwareone/System_User.cpp:2803 (extern bool gTimeSyncedMarkerWritten), System_User.cpp:2804 (extern void getTimestampPrefixMsCached), System_User.cpp:2805 (extern bool appendLineWithCap), components/hardwareone/System_Filesystem.cpp:33 (extern void getTimestampPrefixMsCached), System_Filesystem.cpp:35 (extern time_t computeNextRunTime)
- Evidence: Each symbol appears in its TU ONLY at the declaration line (grep-verified per TU). Example, System_User.cpp:2803-2805:
extern bool gTimeSyncedMarkerWritten;
extern void getTimestampPrefixMsCached(char* buf, size_t bufSize);
extern bool appendLineWithCap(const char* path, const String& line, size_t cap);
A fourth line in this block (extern void timeSyncUpdateBootEpoch()) was deleted by the parallel session mid-sweep — these three survived it. System_Filesystem.cpp:35's computeNextRunTime has no use anywhere in that 1959-line file (the neighboring externs at 32/34/36-38 ARE used).
- Recommendation: Delete all five lines. They are also all redundant even if a use appeared later: gTimeSyncedMarkerWritten is declared in System_Logging.h:61 (System_User.cpp includes it at line 21), getTimestampPrefixMsCached in System_Utils.h:372, appendLineWithCap in System_Filesystem.h:209, computeNextRunTime in System_Automation.h:86 — headers already included (or trivially includable) by these TUs.
- Risk: Zero behavioral risk (declarations only). Sequence with the parallel session's edits to System_User.cpp to avoid merge noise — it edited the adjacent line during this sweep.

## [DONE] [hazard] System_WiFi.cpp:47 stale-signature extern of syncNTPAndResolve now collides with the rewritten header declaration
- Occurrences: components/hardwareone/System_WiFi.cpp:47 (extern), System_WiFi.cpp:317 and 1187 (zero-arg call sites), components/hardwareone/System_Utils.h:190 (current declaration), components/hardwareone/System_Utils.cpp:2398 (current definition)
- Evidence: System_WiFi.cpp:47: extern bool syncNTPAndResolve();  // Synchronous NTP sync
System_Utils.h:190 (included by System_WiFi.cpp at line 17): bool syncNTPAndResolve(NtpSyncOutcome* outcomeOut = nullptr);
The parallel session changed the signature mid-sweep; the local extern was not updated. The TU now sees two overloads; the no-arg one has no definition anywhere, so syncNTPAndResolve() at lines 317/1187 is at best an ambiguous-overload compile error and at worst an undefined-reference link error.
- Recommendation: Delete System_WiFi.cpp:47 and rely on the System_Utils.h declaration (already included). This is the live proof of why the local-extern pattern must go: I watched the same class of break appear and get fixed in System_Utils.cpp (a no-arg extern of logTimeSyncedMarkerIfReady) within this sweep. Surface to the implementing session immediately — the tree likely does not compile until this line is fixed or their edit reaches it.
- Risk: WiFi-gated TU (whole file body under #if ENABLE_WIFI), so a non-WiFi board build would NOT catch it — the board-gating footgun on record. FeatherS3 (primary) builds WiFi on, so the next build will hit it.

## [BACKLOG] [duplicate] Redundant local externs shadowing header declarations across seven TUs (time/logging helpers)
- Occurrences: components/hardwareone/WebServer_Server.cpp:391 AND 1214 (appendLineWithCap declared twice in one TU), WebServer_Server.cpp:1215 (getTimestampPrefixMsCached), components/hardwareone/System_Debug.cpp:2302-2303 (both), components/hardwareone/System_Filesystem.cpp:32 (readText), components/hardwareone/System_SensorLogging.cpp:64 (getTimestampPrefixMsCached), components/hardwareone/System_User.cpp:2801-2802 (gNTPAnchorId/gBootCounter, duplicating the same TU's own externs at lines 124-125), components/hardwareone/System_Utils.cpp:2310-2312 (resolvePendingUserCreationTimes/writeBootAnchor/logTimeSyncedMarkerIfReady), components/hardwareone/System_TaskUtils.cpp:33 and components/hardwareone/System_FirstTimeSetup.cpp:69 (load-bearing only because those TUs skip the header include)
- Evidence: Every one of these TUs except System_TaskUtils.cpp and System_FirstTimeSetup.cpp already #includes the header that declares the symbol: getTimestampPrefixMsCached → System_Utils.h:372, appendLineWithCap → System_Filesystem.h:209, readText → System_Utils.h:211, resolvePendingUserCreationTimes/writeBootAnchor → System_User.h:195-196 (System_Utils.cpp includes System_User.h at line 34), logTimeSyncedMarkerIfReady → System_Logging.h:65. WebServer_Server.cpp even uses getTimestampPrefixMsCached at line 404 — 800 lines BEFORE its local extern at 1215 — proving the header declaration is what actually compiles it.
- Recommendation: Delete every redundant extern; for the two load-bearing ones, replace with the proper #include (System_TaskUtils.cpp → System_Utils.h, System_FirstTimeSetup.cpp → System_User.h, System_Utils.cpp → System_Logging.h). Single copy lives in the owning headers, per the repo convention. Exception: System_Clock.cpp:16-24's externs are deliberate and documented ("Duty targets that always link" — avoids include cycles); leave those or apply the same doc pattern if kept elsewhere.
- Risk: Declarations only — zero behavioral change. The payoff is real: signature drift then becomes a single-header edit instead of the per-TU hunt that produced the System_WiFi.cpp:47 break.

## [BACKLOG] [duplicate] Hand-rolled UTC→local conversion + weekday algorithm duplicated between the DS3231 driver and the RTC web endpoint (with a stale watchdog-era comment)
- Occurrences: components/hardwareone/i2csensor_ds3231.cpp:303-350 (rtcDaysInMonth + rtcDayOfWeek [Sakamoto] + rtcLocalTime — the shared, exported copy, i2csensor_ds3231.h:87), components/hardwareone/WebPage_Sensors.cpp:469-541 (inline second copy in the /api RTC handler: minute-offset math, two leap-year daysInMonth tables at 493 and 503, day/month/year rollover, Zeller's congruence weekday at 520-533), stale comment at WebPage_Sensors.cpp:470
- Evidence: WebPage_Sensors.cpp:469-470:
// RTC stores UTC time - convert to local time using gSettings.tzOffsetMinutes
// Manual calculation to avoid unsafe setenv/tzset that causes watchdog timeout
...then ~70 lines re-implementing exactly what rtcLocalTime() already does, using a DIFFERENT weekday algorithm (Zeller vs Sakamoto) for the same job. The comment's rationale is dead: Clock::applyTimezone() (System_Clock.cpp:68) now sets TZ once at boot on every boot path, and the one remaining setenv site (rtcDateTimeToEpochUtc) is watchdog-safe.
- Recommendation: Replace the WebPage_Sensors.cpp block with either (a) RTCDateTime local = rtcLocalTime(&dt) — already exported, zero new surface — or better (b) time_t t = rtcToUnixTime(&dt); localtime_r(&t, &tm) now that TZ is trustworthy, which also retires rtcLocalTime/rtcDaysInMonth/rtcDayOfWeek entirely (their only other consumers are cmd_rtc at i2csensor_ds3231.cpp:635 and i2csensor_ds3231_oled.h:37, both equally convertible). Single copy home: the epoch path through libc, per the clock plan's direction. Delete the line-470 comment either way.
- Risk: Both sites are #if ENABLE_RTC_SENSOR gated (WebPage_Sensors.cpp gate at 439), so a non-RTC build hides breakage — build with the RTC board flag on. Handler runs on the httpd task: localtime_r is fine there. Behavior nit to preserve or knowingly fix: the web copy emits UTC seconds alongside local H:M (dt.second passthrough), identical either way.

## [BACKLOG] [duplicate] BOOT_ANCHORS_FILE path literal re-rolled as a private static in the migration tool
- Occurrences: components/hardwareone/System_User.cpp:2798 (const char* BOOT_ANCHORS_FILE = "/system/boot_anchors.json"; — non-static global, but not declared in any header), components/hardwareone/WebServer_MigrationTool.cpp:68 (static const char* BOOT_ANCHORS_FILE = "/system/boot_anchors.json";)
- Evidence: WebServer_MigrationTool.cpp:68: static const char* BOOT_ANCHORS_FILE  = "/system/boot_anchors.json";  // pending-user NTP anchors (split out of users.json)
WebServer_MigrationTool.cpp:412: addFileToBackup(files, warnings, BOOT_ANCHORS_FILE);
If the canonical path in System_User.cpp ever moves, backup/restore silently stops covering the anchors file — no compile error, pending-user timestamps become unresolvable after restore.
- Recommendation: Declare extern const char* BOOT_ANCHORS_FILE; in System_User.h next to resolvePendingUserCreationTimes()/writeBootAnchor() (the file's other consumers already live there), delete the static copy in WebServer_MigrationTool.cpp. Same treatment USERS_JSON_FILE already gets ("exported for use by other modules", System_User.cpp:2790).
- Risk: None — string identity today; this is drift-proofing.

## [BACKLOG] [inconsistency] Two parallel boot-anchor stores: full map, one real policy divergence (upsert vs write-once), and the merge call
- Occurrences: System A: components/hardwareone/System_User.cpp:2798 (BOOT_ANCHORS_FILE=/system/boot_anchors.json), writeBootAnchor at 3326-3372 (UPSERT by gNTPAnchorId, cap 16, JSON, writeTextAtomic), readBootAnchors:2956, cleanupOldBootAnchors:3146 (prune-to-1 once no user pending), consumer resolvePendingUserCreationTimes:3196. System B: components/hardwareone/System_TimeAnchors.cpp:28 (CAPTURE_DIR_SENSORS/.anchors.csv), writeAnchorLine:132 (append-ONCE per boot, keyed gBootCounter, cap 32, CSV), consumer promoteFolderPass:196 (retro-dates boot-named capture files), pruned per promoted folder (registryPrune:117). Policy divergence trigger: components/hardwareone/System_Clock.cpp:213-214 pends PEND_ANCHOR+PEND_RESOLVE on any valid→valid step >120 s — which refreshes System A only; System B's once-per-boot latch is at System_TimeAnchors.cpp:389-394 (gAnchoredThisBoot + registryFind guard) and nothing ever updates an existing CSV row.
- Evidence: WHAT EACH DOES — A: maps ntpAnchorId→(epochAtSync,millisAtSync) so users created pre-sync (users.json rows carry ntpAnchorId+createdMs+bootCount) get real createdAt once a clock arrives; gNTPAnchorId is highest-persisted+1 per boot (System_User.cpp:3458,3494), so consecutive never-synced boots SHARE an id — the bootCount tiebreak at 3294-3301 exists to disambiguate. B: maps bootCounter→(msAtSync,epochAtSync) so dark-boot capture files named boot-N/…-bootN-<ms>.csv are renamed into YYYY-MM-DD/ folders via fileEpoch = epochAtSync - (msAtSync - fms)/1000 (System_TimeAnchors.cpp:250-252). CONFUSION: none found — no shared keys, files, or readers (verified by grep). DIVERGENCE: after a ring adoption (SYNC_RING) followed by an NTP true-up (440 s correction observed in the field per the Health-Trends run), A's row is corrected by the PEND_ANCHOR upsert; B's row keeps the ring-derived epoch forever, so every capture promoted afterward in that boot is mis-dated by the correction — up to minutes, enough to land files in the wrong YYYY-MM-DD folder near midnight (the 40-day sanity window at :46 never catches it).
- Recommendation: MERGE CALL — feasible but rule it out for this pass, and say so in both file headers. Honest assessment: a single store keyed by gBootCounter could serve both consumers (users.json rows already persist bootCount; bootCount is strictly-increasing NVS, a STRONGER key than the aliasing ntpAnchorId; the no-backwards-compat rule makes the file-format change free), retiring ~150 lines of parallel scaffolding and the anchor-id aliasing wart. But it rewires the hand-rolled string-surgery resolution path in System_User.cpp mid-flight with the clock plan, for zero user-visible gain — wrong moment. DO take the narrow fix in this pass: give System B a refresh hook on corrective steps — an upsert-by-boot rewrite of this boot's CSV row (mirror writeBootAnchor's idempotent pattern), invoked from the same main-loop lap that drains PEND_ANCHOR (e.g., clear gAnchoredThisBoot from a Clock duty, or expose timeAnchorsRefresh() called next to clockDutiesTick). Add cross-referencing comments in System_TimeAnchors.h and System_User.cpp:2793 naming each other and the key difference.
- Risk: Coordinate with the parallel session — System_Clock.cpp's duty table is theirs and G2_Ring.cpp's adoption path (the source of the divergent epochs) is uncommitted. Both consumers already run main-loop context with FsLockGuard, so a refresh hook adds no new task-context hazard. Files promoted BEFORE the correction keep their old names — accept the split or leave it; renaming them retroactively is not worth it.

## [DONE] [inconsistency] Hand-rolled epoch-validity literals surviving alongside Clock::isValidEpoch (plus an upper-bound idiom worth absorbing)
- Occurrences: components/hardwareone/System_CrashRecord.cpp:418 (now > 1600000000), components/hardwareone/System_ImageManager.cpp:138 (now > 1704067200), components/hardwareone/System_ESPNow.cpp:3672 (nowT > 1577836800), components/hardwareone/System_User.cpp:3097 (createdAtUtc < 1577836800), components/hardwareone/HardwareOne.cpp:1782 (timeinfo.tm_year >= 120, deliberately inline — has a comment explaining why Clock::isSynced would race), upper-bound pairs: components/hardwareone/G2_Ring.cpp:411,420 and components/hardwareone/System_Utils.cpp:2006 (Clock::isValidEpoch(t) && t < 4102444800), components/hardwareone/System_G2_Protocol.cpp:1767-1768 (TS_MIN=1577836800u/TS_MAX=4102444800u)
- Evidence: System_CrashRecord.cpp:418: if (now > 1600000000 && w < kCap) {  — a third 1600000000 literal beyond the two already replaced (this one is in the uncommitted crash-persistence work). System_ImageManager.cpp:138: if (now > 1704067200) {  // After 2024-01-01 (valid time). System_User.cpp:3097: if (createdAtUtc < 1577836800) return false;  // sanity: before Jan 2020. Three different epochs (2020/2020-09/2024) currently define "valid time" depending on the file, while System_Clock.h:45 declares isValidEpoch as the one vocabulary.
- Recommendation: Sweep the four bare literals to Clock::isValidEpoch(t) (System_User.cpp:3097 keeps its additional not-in-future check). Leave HardwareOne.cpp:1782 (documented deliberate) and System_G2_Protocol.cpp (wire-contract constants documented in the header charter at System_G2_Protocol.h:1016). Separately: three call sites now hand-pair isValidEpoch with "< 4102444800" — consider giving Clock::isValidEpoch the 2100 upper bound (or adding Clock::isPlausibleEpoch) so the pairing stops being re-rolled; G2_Ring.cpp's two copies are uncommitted, so this is cheap to fold in now.
- Risk: Threshold semantics shift slightly (2024→2020 for ImageManager; 2020-09→2020 for CrashRecord) — all four sites only distinguish "synced vs boot-epoch", where anything ≥2020 is equivalent. CrashRecord runs in panic/early-boot context: Clock::isValidEpoch is gmtime_r + arithmetic, no allocation, safe there.

## [DONE] [hazard] rtcDateTimeToEpochUtc's process-global TZ=UTC0 window can race concurrent local-time formatting on other tasks; and getTimestampPrefixMsCached's name is now a lie
- Occurrences: components/hardwareone/i2csensor_ds3231.cpp:176-179 (the one remaining setenv site, consolidated mid-sweep), concurrent readers: getTimestampPrefixMsCached (System_Utils.cpp:757, localtime_r, called from at least 5 tasks: System_Debug.cpp, System_SensorLogging.cpp:394, System_TaskUtils.cpp:116/387, WebServer_Server.cpp:404/1498), Clock::isSynced/formatISO8601Local (System_Clock.cpp:38/100), System_TimeAnchors.cpp:262
- Evidence: i2csensor_ds3231.cpp:176-179:
  setenv("TZ", "UTC0", 1);
  tzset();
  const time_t t = mktime(&timeinfo);
  Clock::applyTimezone();  // recompose the canonical TZ from settings
Any task that calls localtime_r inside that window formats in UTC — a log-line prefix or an .anchors.csv promotion date stamped with the wrong offset. Pre-existing pattern (previously 3 windows, now 1), newly relevant because TZ is finally meaningful on every boot path. Separately, System_Utils.cpp:757 getTimestampPrefixMsCached no longer caches anything (the comment at 749-756 documents the deleted cache) — the name now actively misleads about its cost/behavior.
- Recommendation: Low priority, note-and-decide: either accept the microsecond-scale window with a comment on rtcDateTimeToEpochUtc naming the exposure, or compute the UTC epoch arithmetically (days-since-epoch from y/m/d + h/m/s — ~10 lines, no TZ mutation) inside the helper and delete the setenv entirely; the helper is the single copy now, so the fix is one place. Rename getTimestampPrefixMsCached → getTimestampPrefixMs in the same pass (mechanical, ~15 call sites + 1 header) or leave with the explanatory comment — but flag it so the misleading name doesn't outlive the redesign that justified it.
- Risk: RTC sync paths run at early boot (before most tasks) and from cmd_exec — the window overlapping a log write is unlikely but real once web/sensor-logging tasks are up. The rename is churn in files the parallel session is actively editing; sequence it last.

## [DONE] [inconsistency] Two hand-rolled 'UTC±H:MM' tz labels, both wrong for negative sub-hour offsets (ds3231's sign fully inverts)
- Occurrences: components/hardwareone/i2csensor_ds3231.cpp:637-652 (rtcread status label); components/hardwareone/System_WiFi.cpp:1206-1207 (cmd_ntpstatus label); correct sign technique already exists at components/hardwareone/System_Clock.cpp:77-91 (applyTimezone)
- Evidence: ds3231:646: `snprintf(tzBuf, sizeof(tzBuf), "UTC%+d:%02d", (offsetMin / 60) * sign, absMin % 60);` — for offsetMin=-330, (−330/60)*(−1)=+5 → prints "UTC+5:30" (sign INVERTED; should be UTC-5:30); for -30 → "UTC+0:30". WiFi:1207: `snprintf(tzStr, sizeof(tzStr), "UTC%+d:%02d", tzMin / 60, abs(tzMin % 60));` — -330 renders correctly but -30 → "UTC+0:30" (sign lost, same truncation footgun System_Clock.cpp:78-82 documents). Verified by arithmetic on the quoted expressions.
- Recommendation: Add Clock::formatTzOffsetLabel(char*, size_t) in System_Clock.cpp using applyTimezone's sign-from-full-offset approach (sign = offset<0, then labs()), and adopt at both sites. This is also the prerequisite for the plan's deferred Stage-3 'effective offset' label change (plan touches these labels only in the deferred DST stage, not the sign bugs).
- Risk: ds3231 site is inside #if ENABLE_RTC_SENSOR — compiled out on FeatherS3 (CUSTOM_ENABLE_RTC 0), so verify with a CUSTOM_ENABLE_RTC 1 compile per the board-gating footgun. Both sites run in cmd_exec task context; helper is pure formatting.

## [BACKLOG] [duplicate] cmd_rtcset duplicates cmd_timeset's parser and diverges: input treated as UTC (rtcset) vs local (timeset), plus 32-bit toInt overflow and no plausibility gate
- Occurrences: components/hardwareone/i2csensor_ds3231.cpp:676-744 (cmd_rtcset: digit-detect :697-703, `arg.toInt()` :706, sscanf :711, verbatim field write :716-721); components/hardwareone/System_Utils.cpp:1957-2032 (cmd_timeset: digit-detect :1971-1976, strtoll :1979-1988, sscanf :1994-1996, plausibility gate :2008)
- Evidence: rtcset stores parsed civil fields straight into the RTC, which stores UTC (`dt.year = year; ... rtcWriteDateTime(&dt)`), while timeset's identical-format input goes through `t = mktime(&timeinfo)` under the ambient TZ, i.e. LOCAL — `timeset 2026-07-30 12:00:00` and `rtcset 2026-07-30 12:00:00` load different absolute times into the RTC. rtcset also kept `uint32_t unixTime = arg.toInt();` (32-bit, wraps past 2038) and has no rtcDateTimePlausible/isValidEpoch gate, both of which cmd_timeset just gained (System_Utils.cpp:1979-1988, :2008). The plan's P1.2c fixed only timeset.
- Recommendation: Extract one shared parse helper (all-digit strtoll + range check, else sscanf) — natural home next to cmd_timeset in System_Utils.cpp or a small Clock::parseEpochOrCivil — and either make rtcset delegate to the timeset core followed by rtcSyncFromSystem(), or explicitly document/keep the UTC-input semantics and reuse rtcDateTimePlausible (i2csensor_ds3231.cpp:185) before rtcWriteDateTime.
- Risk: Board-gated ENABLE_RTC_SENSOR — a FeatherS3 build stays green even if this file breaks; compile-check with CUSTOM_ENABLE_RTC 1. Changing rtcset's input semantics is user-visible; needs a decision, not a silent unification.

## [BACKLOG] [duplicate] WebPage_Sensors carries a full second implementation of RTC UTC→local conversion (rtcLocalTime), with its own leap-year table and a different day-of-week algorithm
- Occurrences: components/hardwareone/WebPage_Sensors.cpp:469-536 (inline: rollover :482-515, Zeller :520-533); components/hardwareone/i2csensor_ds3231.cpp:316-356 (rtcLocalTime, Sakamoto + rtcDaysInMonth); third consumer i2csensor_ds3231_oled.h:37 already calls rtcLocalTime
- Evidence: WebPage_Sensors.cpp:469-470: "RTC stores UTC time - convert to local time using gSettings.tzOffsetMinutes / Manual calculation to avoid unsafe setenv/tzset that causes watchdog timeout" followed by ~65 lines duplicating rtcLocalTime's totalMinutes/day-rollover/leap-year logic plus Zeller's congruence; rtcLocalTime does the same job via rtcDaysInMonth/rtcDayOfWeek (Sakamoto). The justifying comment predates rtcLocalTime, which performs no setenv/tzset at all.
- Recommendation: Replace the inline block with `RTCDateTime local = rtcLocalTime(&dt);` (both sites are inside #if ENABLE_RTC_SENSOR, same TU dependency already exists via the header). The plan parks this file's rewrite in deferred DST Stage 3 (D2.3, days_from_civil); the pure dedup does not need to wait for DST and removes one of the two calendar-math copies now. Mind rtcLocalTime's dayOfWeek is 1=Sun..7 vs the handler's 0=Sun index into days[].
- Risk: Board-gated ENABLE_RTC_SENSOR (compiled out on FeatherS3) — needs the RTC-board compile check. Runs on the httpd task; rtcLocalTime is pure arithmetic, no stack/latency concern.

## [DONE] [inconsistency] Seven surviving hand-rolled epoch-validity checks with five different thresholds (2001, 2020, 2021, 2024) where Clock::isValidEpoch/isSynced exist
- Occurrences: components/hardwareone/System_Debug.cpp:1693 (`now > 0 && now > 1000000000` → 2001); System_Maps.cpp:2946 (`now > 1609459200` → 2021); System_ESPNow.cpp:10000 (`(now > 1609459200) ? now : 0`); System_ImageManager.cpp:138 (`now > 1704067200` → 2024); System_CrashRecord.cpp:418 (`now > 1600000000` → 2020-09); System_ESPNow.cpp:3672 (`nowT > 1577836800` → 2020-01); System_WiFi.cpp:1214 (`timeinfo.tm_year + 1900 >= 2020` in cmd_ntpstatus)
- Evidence: All verified by reading each site. Examples: System_CrashRecord.cpp:418 `if (now > 1600000000 && w < kCap) {` — the third 1600000000 literal the earlier isValidEpoch sweep missed (the known-handled note says two were replaced); System_ImageManager.cpp:138 `if (now > 1704067200) {  // After 2024-01-01 (valid time)`; System_Debug.cpp:1693 accepts 2002-era garbage the rest of the firmware rejects. Same 'is the clock real' question, five answers.
- Recommendation: Replace each comparison with Clock::isValidEpoch(now) (System_Clock.h:45); System_WiFi.cpp:1214 can use the tm it already has or Clock::isSynced(). While touching System_Debug.cpp:1694, System_Maps.cpp:2947, System_ImageManager.cpp:139 and WebServer_Server.cpp:4957, also switch non-reentrant `localtime(&now)` to `localtime_r` to match the codebase convention (ESP-IDF newlib makes localtime per-task-reent so it is drift, not an active race).
- Risk: Thresholds all move to 2020 — behavioral delta is benign (accepts 2020-2023 where a site demanded 2024, rejects 2002-2019 where Debug accepted it). System_CrashRecord runs very early in boot: Clock::isValidEpoch is pure libc math, safe there. HardwareOne.cpp:1782's tm_year>=120 is NOT in this list — it carries an explicit race-avoidance justification comment (:1773-1779) and should stay.

## [DONE] [duplicate] 2100-01-01 epoch ceiling literal 4102444800 repeated 4x (plus a fifth vocabulary as year<=2099), no shared constant
- Occurrences: components/hardwareone/G2_Ring.cpp:411 and :420 (ringBestKnownEpoch caps); System_G2_Protocol.cpp:1768 (`static const uint32_t TS_MAX = 4102444800u;`); System_Utils.cpp:2008 (cmd_timeset gate `t >= 4102444800LL /* 2100-01-01 */`); same policy as year fields at i2csensor_ds3231.cpp:186 (`dt->year >= 2020 && dt->year <= 2099`)
- Evidence: G2_Ring.cpp:411: `if (!Clock::isValidEpoch((time_t)ts) || ts >= 4102444800u /* 2100-01-01 */) return 0;` — the `isValidEpoch(x) || x >= 4102444800` pair is now the de-facto plausibility idiom, spelled out longhand at every site because isValidEpoch has no upper bound (System_Clock.cpp:42-47).
- Recommendation: Add `Clock::isPlausibleEpoch(time_t)` (isValidEpoch + upper bound) or a `Clock::kEpoch2100` constant in System_Clock.h and use it at all four epoch sites; derive G2_Protocol's TS_MAX from it. rtcDateTimePlausible stays (it validates civil fields pre-conversion) but its comment can point at the shared constant.
- Risk: G2_Protocol site validates wire frames for the glasses — keep uint32 comparison semantics. All call sites are main-loop or cmd_exec context; pure computation.

## [BACKLOG] [duplicate] UTC ISO-8601 'Z' timestamp formatting implemented 4 times (two private static helpers + two inlines)
- Occurrences: components/hardwareone/System_User.cpp:2910-2927 (static formatEpochAsISO8601, writes "null" on invalid); System_User.cpp:1103-1106 (inline in updateUserLastSeen, own isValidEpoch+gmtime_r+strftime); System_ESPNow.cpp:3669-3674 (inline, own 1577836800 gate); System_R1_Protocol.cpp:422-431 (static formatEpoch, "n/a" sentinel)
- Evidence: All four emit `strftime(..., "%Y-%m-%dT%H:%M:%SZ", &tmUtc)` from gmtime_r with slightly different guard/sentinel wrappers. System_User.cpp even duplicates itself: updateUserLastSeen at :1103-1106 hand-rolls what formatEpochAsISO8601 at :2910 already does in the same file.
- Recommendation: Add Clock::formatISO8601UTC(time_t, char*, size_t) (mirror of the existing-but-unused formatISO8601Local, gmtime_r instead of localtime_r) as the single copy; the four sites keep their own sentinel choices ("null"/"n/a"/skip-field) around the call. Fixes System_ESPNow's odd 1577836800 gate for free (overlaps the validity-literal item).
- Risk: These strings are persisted (users.json createdAt/lastSeen) and sent over ESP-NOW/R1 — format must stay byte-identical, which it is. No board gating; R1 site is called from command/status paths, helper is pure.

## [BACKLOG] [duplicate] Filename timestamp format "%Y-%m-%dT%H-%M-%S" hand-rolled at 5 sites (plus one "_"-separator variant), each with its own validity gate
- Occurrences: components/hardwareone/System_Debug.cpp:1697; System_Maps.cpp:2948; System_SensorLogging.cpp:337 and :1907 (with "%Y-%m-%d" day-dir variants at :929, :1898 and System_TimeAnchors.cpp:264); System_TimeAnchors.cpp:266; WebServer_Server.cpp:4959 (variant "%Y-%m-%d_%H-%M-%S")
- Evidence: Five byte-identical `strftime(stamp, sizeof(stamp), "%Y-%m-%dT%H-%M-%S", ...)` calls, fed by four different clock-validity idioms (see the validity-literal item) and a sixth site that picked a different separator for the same purpose (automations-backup filename).
- Recommendation: One Clock::formatStampLocal(time_t, char*, size_t) emitting exactly "%Y-%m-%dT%H-%M-%S" (and optionally formatDayLocal for "%Y-%m-%d"), adopted at the five identical sites; migrate WebServer's underscore variant only as a deliberate rename. Do NOT reuse the existing Clock::formatFilenameLocal — its "%Y%m%d_%H%M%S" format matches nothing on disk (see dead-helpers item).
- Risk: On-disk naming contract: System_TimeAnchors' promotion sweep and the dated-capture/.anchors.csv machinery compare and parse these names — the helper must be byte-identical to today's format, dedup only, zero format change. Anchor-upsert work in the ring session is touching the same files; coordinate.

## [DONE] [inconsistency] HardwareOne early resolvePendingUserCreationTimes gated on `time(nullptr) > 0` — an uptime test, not a clock-validity test
- Occurrences: components/hardwareone/HardwareOne.cpp:1496-1499
- Evidence: `// If time is already valid (warm boot, retained RTC), resolve user creation times early
  if (time(nullptr) > 0) {
    resolvePendingUserCreationTimes();
  }` — the system clock counts up from epoch 0, so this passes ~1s into every boot (the plan's own audit note, doc line 321, confirms) and the call runs with a 1970 clock on cold boots, doing nothing useful; every other site now says Clock::isValidEpoch/isSynced.
- Recommendation: Gate on Clock::isSynced() — that keeps the one case the new clockStepped chokepoint cannot cover (time carried across a soft reboot in the RTC domain: no settimeofday happens, so no step, no PEND_RESOLVE; the ledger's SYNC_NONE+synced state is exactly this). If the parallel session adds a carryover duty elsewhere, delete the block instead. Either way the `> 0` test should not survive the pass.
- Risk: setup() context after initFilesystem (:1337) — users.json I/O is safe here per the plan's boot-order verification. Semantics choice (isSynced vs delete) belongs to the plan implementer; flagging because the plan records the observation but schedules no fix.

## [BACKLOG] [hazard] batteryLogAppend stamps CSV rows with no clock-validity gate — pre-sync rows carry 1970 datetimes
- Occurrences: components/hardwareone/System_Battery.cpp:645-651
- Evidence: `const time_t epoch = time(nullptr);
  char dt[20];
  struct tm tmv;
  localtime_r(&epoch, &tmv);
  strftime(dt, sizeof(dt), "%Y-%m-%d %H:%M:%S", &tmv);` — no isValidEpoch check, unlike every sibling logger (sensor-log stamps fall back to millis, log prefixes return ""). Rows logged before first sync get "1970-01-01 00:00:XX" in both epoch_s and datetime columns.
- Recommendation: Gate with Clock::isValidEpoch(epoch): write empty datetime cell (and 0 epoch_s) so pre-sync rows are recognizable; the row already carries boot+uptime_ms for attribution, matching the sensor-log convention.
- Risk: Keep the column count identical (emit empty string, not fewer commas) — battery CSV is machine-parsed. Low severity: data is odd-looking, not lost.

## [DONE] [dead] nowEpoch() — zero callers, duplicates Clock::epochSeconds()
- Occurrences: components/hardwareone/System_Utils.h:201 (decl); System_Utils.cpp:2516-2518 (def)
- Evidence: `time_t nowEpoch() {
  return time(nullptr);
}` — repo-wide grep finds only the decl and def; Clock::epochSeconds() (System_Clock.h:34) is the same one-liner with documentation.
- Recommendation: Delete both lines. Single copy: Clock::epochSeconds().
- Risk: None — no callers, no board gating. Re-verify zero callers at edit time per the audit convention (tree is moving).

## [DONE] [dead] formatDateTime() — zero callers, non-reentrant localtime(), no validity gate
- Occurrences: components/hardwareone/System_Utils.h:356 (decl); System_Utils.cpp:675-680 (def)
- Evidence: `String formatDateTime(time_t timestamp) {
  struct tm* timeinfo = localtime(&timestamp);
  char buffer[32];
  strftime(buffer, sizeof(buffer), "%Y-%m-%d %H:%M:%S", timeinfo);
  return String(buffer);
}` — repo-wide grep finds no call sites. Would format epoch-0 as a 1970 string if ever used, and returns heap String against the fragmentation guidelines.
- Recommendation: Delete decl + def. If a space-separated local display format is later wanted, add it to Clock:: next to formatISO8601Local with the standard validity handling.
- Risk: None — no callers.

## [BACKLOG] [dead] All three Clock formatting helpers (formatISO8601Local / formatFilenameLocal / formatHHMMLocal) have zero callers while 15+ sites hand-roll the same strftime work
- Occurrences: components/hardwareone/System_Clock.h:104, :109, :113 (decls); System_Clock.cpp:96-126 (defs); intended consumers are the sites listed in the filename-stamp and ISO-Z items
- Evidence: Repo-wide grep for Clock::formatISO8601Local|formatFilenameLocal|formatHHMMLocal matches only System_Clock.h/.cpp. formatFilenameLocal's "%Y%m%d_%H%M%S" format matches no existing on-disk name (the fleet uses "%Y-%m-%dT%H-%M-%S"), so it cannot be adopted as-is without renaming files.
- Recommendation: Resolve during the dedup items above: keep formatISO8601Local and adopt it where "%Y-%m-%dT%H:%M:%S" local display is built (e.g. cmd_time text path), retarget formatFilenameLocal's format string to the fleet's actual "%Y-%m-%dT%H-%M-%S" (or delete it in favor of a new formatStampLocal), and either adopt formatHHMMLocal on the OLED/status clock faces or delete it. Helpers that stay unused after the pass should be deleted — dead API invites drift.
- Risk: None until adopted; on adoption, the filename-format byte-compatibility risk from the stamp item applies.

## [BACKLOG] [inconsistency] Time-domain globals re-externed ad hoc at call sites instead of one owning header — the pattern that let a stale no-arg extern of logTimeSyncedMarkerIfReady linger
- Occurrences: components/hardwareone/System_WiFi.cpp:1200-1202 (extern gNTPAnchorId/gBootCounter/gTimeSyncedMarkerWritten inside cmd_ntpstatus); System_User.cpp:124-125 and :2801-2803 (same globals externed twice in one file); System_Debug.cpp:2303 (extern getTimestampPrefixMsCached); System_SetupWizardMode.cpp:805 (extern applySettings); owning decls: System_Logging.h:61 (gTimeSyncedMarkerWritten), System_Logging.h:65 (logTimeSyncedMarkerIfReady)
- Evidence: System_WiFi.cpp:47-48's own comment records the failure mode this already caused this week: "its signature grew an NtpSyncOutcome out-param and a mismatched local extern" — signature drift is invisible to the compiler when every consumer re-declares locally. gTimeSyncedMarkerWritten is declared in System_Logging.h yet re-externed in two other files.
- Recommendation: Declare gNTPAnchorId/gBootCounter in one header (System_User.h next to writeBootAnchor(), or System_BootState.h) and getTimestampPrefixMsCached in System_Utils.h; replace the local externs with includes. Mechanical, zero behavior change; do it in the same pass while the signatures are hot.
- Risk: None; watch include-cycle hygiene in System_Debug.cpp (it deliberately avoids some includes — if cycle-blocked, keep the extern but add a comment pointing at the owning header).

## [DONE] [inconsistency] cmd_time reports UTC when RTC is the source but LOCAL when NTP is — same field, tz-shifted answer
- Occurrences: components/hardwareone/System_Utils.cpp:1874-1876 (JSON rtc branch), System_Utils.cpp:1882-1884 (JSON ntp branch), System_Utils.cpp:1908-1913 (text rtc branch), System_Utils.cpp:1921-1925 (text ntp branch); correct existing helper at i2csensor_ds3231.cpp:632-653 (rtcLocalTime + tz label)
- Evidence: RTC branch prints raw DS3231 registers: `snprintf(timeBuf,...,"%04d-%02d-%02dT%02d:%02d:%02d", dt.year, dt.month, ...)` — rtcReadDateTime (i2csensor_ds3231.cpp:105-122) returns raw registers, and the DS3231 stores UTC ("RTC stores UTC, not local time", i2csensor_ds3231.cpp:255). NTP branch uses getLocalTime+strftime = LOCAL. Same "time" JSON key and same "Time:" line flip between UTC and local depending on which source answers. The `rtc` command already does this right: `RTCDateTime local = rtcLocalTime(&utc)` plus an explicit tz label.
- Recommendation: In cmd_time, convert the RTC read through the existing rtcLocalTime() (or drop the live-register formatting and format Clock::epochSeconds via localtime_r) so both branches emit local time. Keep the LIVE RTC read itself — that is deliberate (memory: rtc-live-read); only the missing UTC→local conversion is the bug. Single copy of the conversion lives in i2csensor_ds3231.cpp already.
- Risk: cmd_exec task; ENABLE_RTC_SENSOR-gated branch, so a board without the RTC compiled in never shows the bug — test on a build with the DS3231 enabled and tz != 0. `time json` consumers (web/phone) will see the value shift by the tz offset after the fix.

## [BACKLOG] [dead] Mesh TIME_SYNC receive path is write-only: gTimeOffset/gTimeIsSynced never move the clock, contradicting their own comments
- Occurrences: components/hardwareone/System_ESPNow.cpp:343-345 (globals), 2821-2838 (v4h_time_sync handler), 2803 and 4953-4957 (comments claiming it "moves the clock"), 12261-12285 (cmd_espnow_timesync sender), 12288-12304 (cmd_espnow_timestatus reader), 2386-2396 (v4_broadcast_time_sync)
- Evidence: Handler: `gTimeOffset = (int64_t)ts->timeOffset; gTimeIsSynced = true;` — grep shows gTimeOffset is referenced nowhere else except its own declaration and a debug print; no settimeofday, no Clock:: call. Yet the opcode table says "TIME_SYNC moves the device clock — REQ_AUTHENTICATED to defend against..." (line 4953) and the flag doc says "TIME_SYNC moves the clock" (2803). cmd_espnow_timestatus then reports "Synced: Yes  Epoch: <raw time(nullptr)>" — the LOCAL clock, possibly 1970, labeled synced because a frame once arrived. Sender guard is also wrong: `if (epoch < 100000) return Error` (12269) passes after ~28h of unsynced uptime and broadcasts a bogus epoch mesh-wide (harmless today only because receivers discard it).
- Recommendation: Delete the whole vestigial path (globals, handler body, both CLI commands, V4PayloadTimeSync send helper) or reimplement the receive side as a real clock source: settimeofday + Clock::clockStepped(SYNC_x) through the new chokepoint, with Clock::isValidEpoch on both the sender guard and the received epoch. Either way fix the two comments — they currently document behavior that does not exist. Coordinate with the parallel clock-plumbing session before choosing delete vs reimplement.
- Risk: Opcode 35 stays reserved in Wire.h either way (opcode charter). memory says re-verify dead code before deleting — verified here by grep: gTimeOffset has zero readers. espnowtimesync/espnowtimestatus are user-documented CLI commands; removing them needs help-text cleanup.

## [BACKLOG] [hazard] Peer-identity epochs (bondedAtSec/lastSeenSec/createdAtSec) stamped with boot-uptime pre-sync, and bondedAtSec then persists forever
- Occurrences: components/hardwareone/System_ESPNow_Handlers_Crypto.cpp:291-292 (KEY_EX_HELLO bondedAt), System_ESPNow_Handlers_Crypto.cpp:396-397 (KEY_EX_REPLY bondedAt), System_ESPNow_Identity.cpp:220-221 (identity createdAtSec), System_ESPNow_Identity.cpp:484-485 (peer lastSeenSec), preserve-once-nonzero logic at System_ESPNow_Identity.cpp:483
- Evidence: `time_t now = time(nullptr); uint32_t bondedAt = (uint32_t)((now > 0) ? now : 0);` — on ESP32 an unsynced clock counts seconds since boot, so `now > 0` is true within ~1s of boot and the "0 = unknown" sentinel almost never happens. A pairing done before clock sync writes bondedAtSec≈47 to disk, and persist explicitly preserves it forever: `if (!p.valid || p.bondedAtSec == 0) p.bondedAtSec = bondedAtSec;` — the heal path only fires on exactly 0, which the broken guard never produces. Fields are documented "Unix epoch" (System_ESPNow_Identity.h:39,108-109).
- Recommendation: Replace all four `(now > 0) ? now : 0` with `Clock::isValidEpoch(now) ? (uint32_t)now : 0` so the 0 sentinel actually fires and the existing bondedAtSec==0 re-stamp heals on the next persist after sync. One-line change per site; the sentinel convention already exists (R1_Protocol formatEpoch prints "n/a" for 0).
- Risk: BTC/espnow task context — Clock::isValidEpoch is cheap (gmtime_r), fine there. Existing identity files on user devices may already carry bogus small epochs; per the no-backwards-compat convention the user erases before flashing, so no migration needed.

## [BACKLOG] [duplicate] Boot notification broadcast twice on every ESP-NOW autostart boot — identical snippet in initEspNow and setup()
- Occurrences: components/hardwareone/System_ESPNow.cpp:9997-10004 (inside initEspNow) and HardwareOne.cpp:2150-2162 (immediately after cmd_espnow_init returns in setup())
- Evidence: Both sites are byte-similar: `time_t now = time(nullptr); uint32_t timestamp = (now > 1609459200) ? now : 0; String bootMsg = buildBootNotification(generateMessageId(), gEspNow->deviceName.c_str(), gBootCounter, timestamp); meshSendBootToPeers(bootMsg); BROADCAST_PRINTF("[ESP-NOW] Boot notification sent...")`. Boot path runs cmd_espnow_init synchronously (non-web transport falls through to `initEspNow()` at System_ESPNow.cpp:10058), initEspNow sends the notification at 9997-10004, then HardwareOne.cpp:2151 checks `gEspNow && gEspNow->initialized` — now true — and sends it again with a fresh msgId.
- Recommendation: Delete the HardwareOne.cpp:2150-2162 block entirely; initEspNow() is the single home (it also covers manual `openespnow`, which the setup() copy never did). While there, replace the shared `now > 1609459200` with Clock::isValidEpoch.
- Risk: Peers see two BOOT frames per boot today; removing one halves that — verify no peer-side logic counts boot frames. loopTask context, no board gating.

## [BACKLOG] [inconsistency] battery.csv rows stamped unguarded — dark boots write 1970 datetimes and uptime-as-epoch in a default-on log
- Occurrences: components/hardwareone/System_Battery.cpp:647-662 (batteryLogAppend); convention it diverges from at System_SensorLogging.cpp:524
- Evidence: `const time_t epoch = time(nullptr); ... localtime_r(&epoch, &tmv); strftime(dt, ..., "%Y-%m-%d %H:%M:%S", &tmv);` then writes `epoch_s` and `datetime` columns with no validity check. batteryLogEnabled defaults true at 60s interval (System_Settings.h:379-380), so a WiFi-less boot fills the CSV with epoch_s≈uptime and datetime=1970-01-01 rows. The sensor-log CSV already solved this: `Clock::isSynced() ? Clock::epochMillis() : (int64_t)millis()` (System_SensorLogging.cpp:524).
- Recommendation: Gate the two wall-time columns on Clock::isValidEpoch — write empty datetime and 0 epoch_s when dark (boot+uptime_ms columns keep rows orderable), matching the sensor-CSV convention. Optionally use Clock::formatISO8601Local once adopted.
- Risk: Main-loop battery tick. Offline graphers of /battery.csv must tolerate the empty column — but they already must tolerate 1970 garbage today, so honest-empty is strictly better.

## [DONE] [inconsistency] Epoch-validity threshold zoo: six different hand-rolled "is the clock real" constants coexist with Clock::isValidEpoch
- Occurrences: 100000 (~28h uptime): System_ESPNow.cpp:12269, System_WiFi.cpp:1346, System_WiFi.cpp:1490, WebServer_Server.cpp:5277; 1000000000 (2001): System_Debug.cpp:1693; 1577836800 (2020): System_ESPNow.cpp:3672, System_User.cpp:3097; 1600000000 (2020-09): System_CrashRecord.cpp:418; 1609459200 (2021): System_Maps.cpp:2946, HardwareOne.cpp:2157, System_ESPNow.cpp:10000; 1704067200 (2024): System_ImageManager.cpp:138; hand-rolled tm_year check: System_WiFi.cpp:1211-1212
- Evidence: Examples: `if (now > 0 && now > 1000000000) { // Valid epoch time (after year 2001)` (System_Debug.cpp:1693); `if (now > 1609459200) { struct tm* ti = localtime(&now);` (System_Maps.cpp:2946); `if (now > 1704067200) { // After 2024-01-01` (System_ImageManager.cpp:138); `if (now > 1600000000 && w < kCap)` (System_CrashRecord.cpp:418); `if (timeinfo.tm_year + 1900 >= 2020)` (System_WiFi.cpp:1212). Clock::isValidEpoch (System_Clock.cpp:26-31) is the declared single vocabulary; a stale-2005 RTC passes the Debug check but fails everywhere else, and an unsynced device >28h up passes every 100000 check.
- Recommendation: Replace every listed comparison with Clock::isValidEpoch(now) (or Clock::isSynced() where the value being tested is `time(nullptr)` itself). System_User.cpp:3097 and System_G2_Protocol.cpp:1767 (TS_MIN, named constant with documented upper bound) are data-range sanity checks on received/derived values — converting them is optional but harmless. Note the exclusion list said the two 1600000000u literals were already replaced; System_CrashRecord.cpp:418's un-suffixed 1600000000 is a third the pass missed.
- Risk: CrashRecord site runs pre-debug-system at boot — Clock::isValidEpoch is safe there (pure libc). WiFi cert sites are ENABLE_WIFI-gated; ImageManager is camera-build-gated (board-gated code hides compile breaks — build both variants).

## [BACKLOG] [inconsistency] Ineffective `time(nullptr) > 0` guards: comments claim "if NTP synced" but the test passes ~1s after any boot
- Occurrences: HardwareOne.cpp:1497, System_User.cpp:1464, System_FirstTimeSetup.cpp:294, System_FirstTimeSetup.cpp:930 (all four gate resolvePendingUserCreationTimes); WebServer_Server.cpp:684-689 (buildAllSessionsJson epoch conversion); System_ESPNow.cpp:6034-6035 (capture filename fallback); G2_Glasses.cpp:15156-15157 and 15285-15286 (mic filename fallback); System_Debug.cpp:2329-2332 (logTimeSyncedMarkerIfReady); System_Automation.cpp:1379-1380 (computeInitialNextAt)
- Evidence: Pattern: `// If NTP already synced, resolve the creation timestamp immediately\n if (time(nullptr) > 0) { resolvePendingUserCreationTimes(); }` — unsynced time() counts seconds since boot, so `> 0` is not a sync test. In buildAllSessionsJson the result is user-visible: `if (now > 0) { epochMillis = (int64_t)now * 1000LL - millis(); }` then `session["createdAt"] = epochMillis + s.createdAt` — pre-sync the admin sessions view renders 1970-era dates instead of the intended raw-millis fallback. The `ts == 0`/`now <= 0` fallbacks (G2 mic, ESP-NOW capture) are dead branches for the same reason: filenames get uptime-that-looks-like-epoch instead of the written fallback.
- Recommendation: One mechanical sweep: replace with Clock::isSynced() (when testing "is the wall clock real now") or Clock::isValidEpoch(t) (when testing a value). The four resolvePendingUserCreationTimes call sites also partially duplicate the new PEND_RESOLVE chokepoint duty in System_Clock.cpp:243 — after the plan lands, the warm-boot call in HardwareOne.cpp:1497 is the only one still needed (with a real guard); confirm with the parallel session before deleting the other three.
- Risk: resolvePendingUserCreationTimes internally sanity-checks (System_User.cpp:3097), so today's damage is wasted I/O plus the 1970 session view. Guard fixes change when boot-time work runs — retest FTS and user-approve flows.

## [BACKLOG] [duplicate] Four hand-rolled epoch→ISO-8601-UTC ("...Z") formatters, each with a different invalid-epoch story
- Occurrences: System_User.cpp:1103-1106 (inline in updateUserLastSeen), System_User.cpp:2910-2927 (static formatEpochAsISO8601, "null" on invalid), System_ESPNow.cpp:3669-3679 (user-sync createdAt, guarded by 1577836800 literal), System_R1_Protocol.cpp:420-431 (static formatEpoch, "n/a" on 0)
- Evidence: All four do `gmtime_r(&t, &tm); strftime(buf, ..., "%Y-%m-%dT%H:%M:%SZ", &tm)`. Two are file-local statics with identical purpose but different sentinels ("null" vs "n/a"); the ESP-NOW copy re-rolls the guard as a magic literal; only updateUserLastSeen uses Clock::isValidEpoch. All writers/readers of users.json createdAt/lastSeen are UTC-coherent (verified: reader formatEpochAsISO8601 and both writers all emit Z; browser parses Z correctly), so this is duplication, not corruption.
- Recommendation: Add Clock::formatISO8601UTC(t, out, outSize) next to the three Local variants (System_Clock.h:104-113) with the same isValidEpoch-empty-string contract, and fold all four sites onto it. The ESP-NOW user-sync copy should arguably call a shared user-creation stamping helper from System_User instead (share-logic convention — it currently re-rolls the whole createdAt/createdAtSource/pending dance that resolvePendingUserCreationTimes owns).
- Risk: users.json createdAt "null"-string vs JSON-null distinction: formatEpochAsISO8601 strncpy's the STRING "null" into a buffer that callers quote — check call sites when unifying so a literal "null" string doesn't become "\"null\"" in JSON. R1 site keeps "n/a" as a display sentinel.

## [BACKLOG] [duplicate] HTTPS cert-expiry check duplicated, and both copies read UTC cert time through mktime (local) while the cert writer correctly uses gmtime_r
- Occurrences: WebServer_Server.cpp:5276-5296 (boot-time check) and System_WiFi.cpp:1345-1364 (certinfo command) — duplicated block; writer at System_WiFi.cpp:1489-1497 (gmtime_r, correct)
- Evidence: Both copies: build `struct tm expTm` from mbedtls `crt.valid_to` fields then `time_t expTime = mktime(&expTm)` — X.509 validity is UTC, but mktime interprets the fields as LOCAL, skewing the comparison by the tz offset (≤14h; immaterial against 30-day warnings but wrong on the boundary day). The self-signed-cert generator in the same file uses `gmtime_r(&now, &tmStart)` for notBefore/notAfter — writer UTC, readers local. Both readers also share the `now < 100000` guard from the threshold zoo.
- Recommendation: Extract one helper (e.g. in System_WiFi.cpp, declared in its header): certValidToEpoch(const mbedtls_x509_crt&) that converts via the timegm workaround already used in i2csensor_ds3231.cpp:197-201 (mktime with TZ=UTC), and have both call sites use it with Clock::isValidEpoch as the gate.
- Risk: Both sites are ENABLE_WIFI/HTTPS-path code — verify on a build with HTTPS certs provisioned. The tz skew is cosmetic today; the duplication is the real cost.

## [DONE] [dead] formatDateTime() has zero callers, and all five Clock:: formatting/age helpers are still unadopted while five sites hand-roll timestamped filenames three different ways
- Occurrences: Dead: System_Utils.cpp:673-678 + declaration System_Utils.h:335. Unadopted (zero callers each, verified by grep): Clock::formatISO8601Local, formatFilenameLocal, formatHHMMLocal (System_Clock.h:104-113), Clock::ageSeconds/ageMs (System_Clock.h:86-94). Hand-rolled filename stamps: System_Debug.cpp:1688-1708 ("%Y-%m-%dT%H-%M-%S", fallback "uptime-%lu"), System_Maps.cpp:2944-2951 (same fmt, fallback "%lu" millis), System_ImageManager.cpp:133-148 ("img_%04d%02d%02d_...", fallback counter), WebServer_Server.cpp:4954-4961 ("%Y-%m-%d_%H-%M-%S", NO fallback/guard at all), System_SensorLogging.cpp:1907 (already Clock-guarded, keeps its own fmt). Hand-rolled age math: System_ESPNow.cpp:11628, System_ESPNow.cpp:12295, System_I2C.cpp:1488, System_Events.cpp:410, G2_Glasses.cpp:5046
- Evidence: formatDateTime: `struct tm* timeinfo = localtime(&timestamp);` — also the only remaining NON-reentrant localtime in a public helper; grep finds no call site. The automations backup handler is the worst hand-roll: `time(&now); struct tm* timeinfo = localtime(&now); strftime(timestamp, ..., "%Y-%m-%d_%H-%M-%S", ...)` — no validity guard, so a pre-sync export downloads as automations-backup-1970-01-01_00-01-05.json, and it invents a fourth filename format (underscore separator).
- Recommendation: Delete formatDateTime + its declaration. Migrate the four unguarded/divergent filename sites onto Clock::formatFilenameLocal (or formatISO8601Local where the T-form is wanted), each keeping its existing millis/counter fallback for the empty-string return. The age-math sites are semantically fine — migrate to Clock::ageSeconds opportunistically, not urgently. This is presumably the adoption pass the plan intended; coordinate so the parallel session doesn't do the same migration twice.
- Risk: Filename-format changes alter on-disk names users may script against; per the no-backwards-compat convention that is acceptable, but the sensor-log day-file naming (System_SensorLogging.cpp:1889-1924) is freshly designed uncommitted work — leave its formats alone and only share the guard.

## [BACKLOG] [inconsistency] Non-reentrant localtime() used at 7 live sites while the rest of the codebase uses localtime_r
- Occurrences: System_Automation.cpp:2860, 2935, 3022; System_Debug.cpp:1694; System_Maps.cpp:2947; System_ImageManager.cpp:139; WebServer_Server.cpp:4957 (plus dead System_Utils.cpp:674)
- Evidence: `struct tm* timeinfo = localtime(&now); int hour = timeinfo->tm_hour;` (System_Automation.cpp:2860-2861) vs the codebase-wide `struct tm tm; localtime_r(&now, &tm)` convention. On ESP-IDF newlib each task has its own _reent so this is not a live cross-task race, but it returns a pointer into per-task static state and is unchecked for NULL at every site.
- Recommendation: Mechanical localtime→localtime_r sweep at the six live sites (the seventh dies with formatDateTime). Most collapse entirely if the sites adopt the Clock:: formatters per the previous item.
- Risk: Style/robustness only — safe to batch with the guard fixes. Automation sites run on loopTask inside evaluateCondition; no board gating.

## [BACKLOG] [inconsistency] JSON key "lastSeenSec" means age-in-seconds in mesh peer metadata but Unix-epoch in the persisted peer identity
- Occurrences: System_ESPNow.cpp:11628+11637 (`uint32_t ageSec = (millis() - lastContact)/1000; ... o["lastSeenSec"] = ageSec;`) vs System_ESPNow_Identity.h:109 (`uint32_t lastSeenSec; // updated by heartbeats` — epoch, written at System_ESPNow_Identity.cpp:485)
- Evidence: Same identifier, same subsystem, opposite semantics: espnowSerializeMeshPeerMeta serializes an AGE under the name the identity layer uses for an EPOCH. users.json also carried a legacy lastSeenSec that updateUserLastSeen now strips (System_User.cpp:1119), a third historical meaning.
- Recommendation: Rename the JSON key in espnowSerializeMeshPeerMeta to "lastSeenAgeSec" (no backwards-compat needed per repo convention) and update the web metadata consumer; keep the identity-file field as the epoch home.
- Risk: Web /api/espnow/metadata and `espnowdevices json` consumers must be updated in the same pass (WebPage_ESPNow.h renders peer.lastSeen — verify which key it binds after the rename).

## [BACKLOG] [hazard] G2 native-notification date formats unguarded epoch — dark-boot notifications carry 19700101T dates to the glasses
- Occurrences: G2_Glasses.cpp:11393-11394 (caller passes unvalidated Clock::epochSeconds()), G2_Glasses.cpp:11347-11353 (g2BuildNotifyJson localtime_r with no isValidEpoch)
- Evidence: `const time_t epoch = Clock::epochSeconds(); String json = g2BuildNotifyJson(...);` and inside: `localtime_r(&epoch, &lt); snprintf(dateBuf, ..., "%04d%02d%02dT%02d%02d%02d", lt.tm_year + 1900, ...)` — no guard anywhere on the path, unlike sibling G2 senders (g2TimeSyncTick guards at G2_Glasses.cpp:8407/8460 with Clock::isValidEpoch; devcfg time is range-checked by the protocol builder per System_G2_Protocol.h:1016).
- Recommendation: Guard at the caller: if !Clock::isValidEpoch, either suppress the date field's value (empty/zero date if the EFS parser tolerates it) or hold native notifications until the clock is real — match whichever convention the notification-overlay work prefers. The byte-stable-CRC comment means the fix must stay inside g2BuildNotifyJson's format, not post-process the JSON.
- Risk: BTC-adjacent send path (BLE write) — keep added work trivial (stack footgun memory). Glasses-side parser behavior with a zero date is unverified; test on hardware before choosing suppress-vs-hold.

## [DONE] [inconsistency] NTP status command hand-rolls its own sync check and blocks up to 10ms doing it
- Occurrences: System_WiFi.cpp:1208-1215
- Evidence: `if (now > 0 && getLocalTime(&timeinfo, 10)) { if (timeinfo.tm_year + 1900 >= 2020) { strftime(...) } }` — a third vocabulary for the same test Clock::isSynced() performs, plus getLocalTime's retry loop (up to 10ms busy-wait) where a single localtime_r suffices, plus the ineffective `now > 0`.
- Recommendation: `if (Clock::isSynced()) { Clock::formatISO8601Local(Clock::epochSeconds(), ...) }` — or the space-separated strftime inline if the historical format matters (the status-JSON site at System_Utils.cpp:1585-1598 documents that exact-format concern and is otherwise the model to copy).
- Risk: cmd_exec task, cosmetic output only. Low.
