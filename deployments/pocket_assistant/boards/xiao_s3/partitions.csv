# Pocket Assistant — Seeed XIAO ESP32-S3 (Sense pin map) — 8 MB flash.
#
# Golden factory recovery + one 5,760 KiB main slot. Direct recovery HTTP
# writes ota_0; it never needs enough LittleFS space to stage the image.
#
# Sizing note: this profile carries the headless service set PLUS Bluedroid,
# the G2/R1 BLE-client stack and the CM5 UART link, so the application is much
# larger than a headless image and the app slot -- not LittleFS -- is the
# binding constraint on 8 MB. ota_0 is therefore given the same 5,760 KiB as
# the 16 MB FeatherS3 recovery layout, and LittleFS keeps 1,216 KiB. That is
# ample for users.json, settings, certs, automations and the system event log;
# bulk capture and the G2 icon packs belong on the Sense microSD card
# (G2_ICON_ANIMATIONS_VFS_PATH is "/sd/...", not LittleFS).
#
# factory 1,144 KiB is well above the ~900 KiB the S3 updater builds to and is
# itself the release gate for this board -- the shared 1.15 MiB gate in
# CMakeLists.txt is larger than the slot, so the slot wins.
#
# This table is exact: 0xA000 through 0x800000 with zero slack. app-partition
# offsets stay 64 KiB aligned; factory's size is chosen so otadata lands
# exactly where it keeps ota_0 on a 64 KiB boundary with no wasted gap.
#
# The stock XIAO layout (partitions_no_sr_8mb.csv) has no factory recovery
# slot and puts LittleFS at 0x5C5000. Installing this layout is cable-only and
# reformats LittleFS; see MIGRATION.md.
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0xA000,    0x4000,
nvs_key,    data, nvs_keys, 0xE000,    0x1000,    encrypted
phy_init,   data, phy,      0xF000,    0x1000,
factory,    app,  factory,  0x10000,   0x11E000,
otadata,    data, ota,      0x12E000,  0x2000,    encrypted
ota_0,      app,  ota_0,    0x130000,  0x5A0000,
littlefs,   data, littlefs, 0x6D0000,  0x130000,  encrypted
