# HardwareOne deployment contract. Values are intentionally shell-free KEY=VALUE
# records so CMake and Python release tooling consume the exact same identity.
DEPLOYMENT_ID=headless
BOARD_ID=feather_esp32_v2
TARGET=esp32
FLASH_SIZE=8MB
OTA_LAYOUT=1
OTA_LAYOUT_ID=hw1-hl-fv2-ota-v1
OTA_VERSION_SUFFIX=fv2ho1
PARTITION_CSV=deployments/headless/boards/feather_esp32_v2/partitions.csv
FEATURE_HEADER=deployments/headless/features.h
MAIN_RELEASE_MAX=0x400000
UPDATER_RELEASE_MAX=0x11E000
FLASH_ENCRYPTION=0
