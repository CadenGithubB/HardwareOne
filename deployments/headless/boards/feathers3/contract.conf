# HardwareOne deployment contract. Values are intentionally shell-free KEY=VALUE
# records so CMake and Python release tooling consume the exact same identity.
DEPLOYMENT_ID=headless
BOARD_ID=feathers3
TARGET=esp32s3
FLASH_SIZE=16MB
OTA_LAYOUT=1
OTA_LAYOUT_ID=hw1-hl-f3-ota-v1
OTA_VERSION_SUFFIX=f3ho1
PARTITION_CSV=deployments/headless/boards/feathers3/partitions.csv
FEATURE_HEADER=deployments/headless/boards/feathers3/features.h
MAIN_RELEASE_MAX=0x400000
UPDATER_RELEASE_MAX=0x126666
FLASH_ENCRYPTION=0
