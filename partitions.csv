# Partition table for builds WITHOUT ESP-SR (no speech recognition)
# No "model" partition — all available space goes to LittleFS
# Factory: 4948K  (matches partitions_sr.csv so a binary built for either
#          target fits in either partition layout.)
# LittleFS: 3180K (~3.1MB — slightly smaller than before to keep the
#          factory partition aligned with the SR variant.)
# Name,   Type, SubType, Offset,   Size,    Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x4D5000,
littlefs, data, littlefs, 0x4E5000, 0x31B000,
